[English](AGENTS.md)

# 项目工作约定

- 仓库工作以 [README](README.zh-CN.md) 和[开发状态](docs/HANDOFF.zh-CN.md)的预先审阅为前提。应用代码位于 `src/SC2Switcher.Wpf`，隔离回归位于 `tests/SC2Switcher.Tests`。
- 开发环境必须使用 Windows x64 和固定的 .NET SDK 10.0.401。SDK 由 `scripts/Setup.ps1` 准备；构建、测试和打包必须使用仓库脚本。
- 测试采用控制台回归程序。验证必须包括退出码和 `TOTAL … PASSED`；没有执行用例的 `dotnet test` 不能替代它。
- 构建、回归和包检查不得启动战网或游戏，也不得安装 MSI。实际安装与在线测试必须分别执行，并记录范围及确切包哈希。
- 应用变更必须保留独立游戏目录、校验、原子写入、备份和恢复检查。界面语言必须独立于游戏语言。
- 英文与简体中文资源必须一致。文档必须采用 `.md` 和 `.zh-CN.md` 配对，互设语言入口，并提供同语言导航。
- 临时输出和原始证据必须位于 Git 忽略的 `artifacts/`，本地工具位于 `.tools/`。提交不得包含真实配置、游戏设置、账号数据、备份或含个人路径的日志。
- 构建脚本必须在执行期间隔离工具环境，结束后恢复。构建失败不得通过添加个人包源凭据或更改用户目录权限解决。
- 验证范围必须由受影响的变更确定。应用大版本发布前需要一次全面验证，后续小幅修订采用针对性检查。文档、许可和包内容变化只需相关内容／包检查，不重复安装、界面或在线矩阵。必要的构建／包检查及既有 CI 检查仍适用。提交前必须通过 `scripts/Check-Repository.ps1`。
- 编译、安装、界面渲染与在线运行必须分别记录。战网本地区域记录不能证明登录成功。
- 每个分发预览必须递增三段版本号。已经打标签或分发的版本必须保留候选原文件，发布提升不得重新构建。
