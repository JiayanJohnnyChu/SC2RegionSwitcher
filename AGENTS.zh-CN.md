[English](AGENTS.md)

# 项目工作约定

- 阅读 [README](README.zh-CN.md) 和[开发状态](docs/HANDOFF.zh-CN.md)。应用代码位于 `src/SC2Switcher.Wpf`，隔离回归位于 `tests/SC2Switcher.Tests`。
- 使用 Windows x64 和固定的 .NET SDK 10.0.401。通过 `scripts/Setup.ps1` 准备 SDK，使用仓库的构建、测试和打包脚本。
- 测试使用控制台回归程序，以退出码和 `TOTAL … PASSED` 为准；没有执行用例的 `dotnet test` 不能替代它。
- 构建、回归和包检查不得启动战网或游戏，也不得安装 MSI。实际安装与在线测试单独执行，记录范围及确切包哈希。
- 修改应用时保留独立游戏目录、校验、原子写入、备份和恢复检查。界面语言保持独立于游戏语言。
- 英文与简体中文资源保持一致。文档采用 `.md` 和 `.zh-CN.md` 配对，互设语言入口，内部导航优先指向同语言版本。
- 临时输出和原始证据放入 Git 忽略的 `artifacts/`，本地工具放入 `.tools/`。不提交真实配置、游戏设置、账号数据、备份或含个人路径的日志。
- 构建脚本在执行期间隔离工具环境，结束后恢复。不要为构建添加个人包源凭据或更改用户目录权限。
- 源码变化后执行适当的构建与回归检查，打包变化后检查包内容。提交前运行 `scripts/Check-Repository.ps1`。
- 编译、安装、界面渲染与在线运行分别记录。战网本地区域记录不能证明登录成功。
- 每个分发预览递增三段版本号。已经打标签或分发的版本保留候选原文件，发布提升不得重新构建。
