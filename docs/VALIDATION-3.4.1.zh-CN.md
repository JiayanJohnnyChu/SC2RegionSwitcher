[English](VALIDATION-3.4.1.md)

# 3.4.1 验证状态

历史记录日期为 2026-09-22。3.4.1 当时作为私有预览版评估，3.4.0 私有草稿在该时点仍暂缓发布。

## 当前全机安装器

采用的方案需要管理员批准，为所有用户安装到 64 位 Program Files，使用 HKLM App Paths 和单一公共开始菜单入口。应用仍为 `asInvoker`，每位用户的配置与备份均不由 MSI 管理。此前的当前用户预览版需一次性卸载重装。设计记录于[安装器说明](../tools/Installer/README.zh-CN.md)。

本地开发全机包已通过以下检查：

| 检查 | 结果 |
| --- | --- |
| 编译 | 零警告、零错误 |
| 隔离回归 | `TOTAL 52 PASSED` |
| 包验证 | 通过 |
| 发布拒绝情形 | 八项全部通过 |
| 独立 ICE 验证 | 零错误、零警告，不屏蔽规则 |

结构证据保存在 `artifacts/installer-schema/e5de63bf487743a48b79fd3fa2156e21/result.json`。这些结果属于本地开发包，不是 CI 候选或安装结果。

### 托管全机生命周期

[CI 运行 35721803273](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35721803273) 的原始候选通过[生命周期运行 35722639637](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35722639637)。

| 标识 | 数值 |
| --- | --- |
| 候选源码 | `208cd6e5186b29a9cd3c00382a4c568ed7ee9f18` |
| 生命周期测试程序源码 | `dc68767ef9b05644ec923bca7d2eaab25c1b4fd2` |
| MSI SHA-256 | `DF85AFD7FAF3F04FE71A21CCD756726966BCCFD773070AEE60B4A27DCAA9931F` |
| ZIP SHA-256 | `BD4E4538B0D9BA5BBD2E26092907D12BEBA400EC36D0C2A2D56D3F7C1737C0D5` |

确切原始 MSI 通过安装、维护、卸载、重装及最终移除。检查覆盖四个已安装文件哈希、HKLM App Paths、公共已播发快捷方式、单一卸载登记，以及用户数据测试文件保持不变。隔离 MSI 副本通过大版本升级和降级拒绝；原始 MSI 针对模拟 HKCU 项通过旧预览版检测。文件占用情形返回 3010，表示操作成功但需要重启。隔离 MSI 副本在旧产品移除后遇到原生错误 1312，触发回滚：旧产品恢复为已安装状态 5，新产品不存在，旧文件哈希与入口均恢复。未记录到伴随系统错误 5 的注册表回滚错误 140x。清理断言全部通过。

这些生命周期结果适用于所标明的包与测试程序。

### 最终候选与本地迁移

最终候选通过 [CI 运行 35723586274](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35723586274)。与托管生命周期源码的比较确认，应用源码与 MSI 定义完全相同，其间变更仅涉及文档和测试程序。

| 标识 | 数值 |
| --- | --- |
| 最终候选源码 | `3337cbacbf2acc0bc8ba9a04c3431cb6e5ab819a` |
| MSI SHA-256 | `C93CF161DFE30ECD2E154B10CD4B9F3B666FC371A553B2E0752FB2F9594B4D7C` |
| ZIP SHA-256 | `C52607AB1193848E226DA5BF38E6351143011A6AFFB9649A18AB87879EF991E5` |

本地迁移使用此原始 MSI。原生 Windows Installer 日志记录了提权请求、成功同意及提权安装。3.4.0 当前用户包移除与 3.4.1 安装均返回零；旧产品不存在，新产品处于已安装状态。四个运行文件均与最终 ZIP 相符；系统保留一个公共开始菜单快捷方式和一个 HKLM 卸载登记，旧用户快捷方式不存在。十六个已有配置与备份文件的哈希保持不变。

已安装应用通过实际开始菜单快捷方式启动，路径对应 Program Files 中的可执行文件，进程保持响应。进程令牌报告 `TokenElevation=false`，确认安装后的运行使用普通用户权限。

已安装应用在已有配置下通过设置窗口的只读英文检查。同一已安装 EXE 另以隔离配置分别启动，通过英文和简体中文设置界面检查。截图和辅助功能树显示控件完整、标签可读，未观察到裁切。这些检查未保存路径、修改已有配置、执行游戏操作或重跑 DPI 矩阵。证据保存在 `artifacts/final-machine-ui/UI_SMOKE_SUMMARY.md`。已完成的完整界面／在线测试仍归属于 3.4.0，不宣称完成了新的在线往返。

## 历史当前用户证据

以下结果早于全机范围改动。管理员对照描述的是旧当前用户包在该权限上下文中的表现，不能证明新全机包通过。

### 本地检查

| 检查 | 结果与范围 |
| --- | --- |
| 编译 | 通过，零警告、零错误。 |
| 隔离回归 | 52 项全部通过。 |
| 包验证 | 包内容及五项发布拒绝情形均通过。 |
| 独立 MSI 结构验证 | 完整标准 ICE 套件，不屏蔽规则，零错误。四条 ICE91 警告涉及固定当前用户目录假设用于全机安装的情形；本包拒绝 ALLUSERS。 |
| 基本界面 | 英文和简体中文，标准 760 × 620 窗口，200% 缩放。选择 EU → US → KR 时国际服卡片相应更新，当前国服状态不变，相关控件均可见。 |

国际服卡片现标示所选战网登录区域。`Core.cs` 和 `ConfigurationStore.cs` 未改。3.4.1 尚无在线游戏往返或完整四档 DPI 矩阵记录；已完成的这些测试属于[原始 3.4.0 候选](VALIDATION.zh-CN.md)。

### 开发 CI

提交 `d3a179bbbb1ec55f9850f1c0846b79d126ffac4d` 通过 [CI 运行 35667524580](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35667524580)，包括编译、回归和包检查。该候选早于当前采用的全机安装方案。

### 当前用户恢复诊断

#### 权限上下文配对比较

[管理员上下文运行 35668661867](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35668661867) 与[标准用户运行 35668664128](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35668664128) 使用相同原始候选和测试程序，均在托管 Windows 11 ARM64 环境运行，系统 build 为 26200，Windows Installer 版本为 5.0.26100.9444。测试采用隔离产品标识，与生产安装分开，检查的是安装器行为，不涉及应用或游戏运行。

| 证据 | 标识 |
| --- | --- |
| 候选源码 | `d3a179bbbb1ec55f9850f1c0846b79d126ffac4d` |
| 恢复工作流与测试程序 | `f512175ef8697632cd240a2df1722ed4c3e8a406` |
| 原始 MSI SHA-256 | `A8C62B46FFA4DF577D6E64CE1EEA1A36025D0012F1D2D530D25A8FE0DACFB89A` |

两次测试都在旧产品移除后到达预定的原生文件复制失败阶段，错误码为 1312。

| 观察项 | 管理员上下文 | 标准用户 |
| --- | --- | --- |
| 注册表操作错误 5（拒绝访问） | 0 次 | 81 次 |
| 实际回滚失败 | 0 次 | 80 次 |
| 旧产品 `ProductState` | 5 → 5，保持已安装 | 5 → 1，从已安装变为已播发 |
| 候选 `ProductState` | -1，不存在 | -1，不存在 |
| 资源恢复 | 四个旧文件哈希、快捷方式、App Paths 和安装器标记均相符；配置测试文件保持不变 | 可见的旧应用资源恢复，但产品登记未恢复 |
| MSI 清理 | 所有资源移除检查通过 | 卸载返回成功，但旧文件、菜单入口、App Paths 和标记仍存在，清理断言失败 |

标准用户测试后，临时账号、配置文件目录和工作目录的移除已完成。管理员测试也完成了清理。两次运行的输入 MSI 原文件均保持不变。单独的后续清理不能将 MSI 清理失败改记为通过。

结果表明，在该环境中恢复失败与权限上下文明显相关，但尚未确定具体原因，也不代表 Windows 缺陷已经官方确认。管理员结果属于诊断对照，不能作为标准用户恢复验收通过的依据。正常 MSI 动作结束返回值未计入回滚失败次数。

#### 较早证据

完整代码与文档提交 `a3d0cf55acb694f31f3c3b3eccef8adb0978339b` 通过 [CI 运行 35664932110](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35664932110)。其 Windows 11 ARM64 [标准用户恢复运行 35666330406](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35666330406) 独立复现了原生故障后从已安装变为已播发的相同变化。更早的 Windows Server [运行 35663705665](https://github.com/JiayanJohnnyChu/SC2RegionSwitcher/actions/runs/35663705665) 在基线安装阶段因策略错误 1625 停止，未进入回滚。

#### 隔离安装器定义实验

本地元数据原型分别尝试仅设置 `WordCount = 2`，以及同时设置 `ALLUSERS = 2`、`MSIINSTALLPERUSER = 1` 和 `WordCount = 2`，均未解决失败。组合设置的原型仍记录到注册表拒绝访问事件。

另一个原型同时使用 `WordCount = 2` 和显式 `Privileged` 启动条件。在静默标准用户测试中，安装于 `InstallInitialize` 之前被拒绝，没有注册表回滚错误，也未留下已安装测试资源。这验证的是提前拒绝，不代表恢复成功。

另行构建的带条件检查 MSI 使用 CI 运行 35664932110 中未改动的应用文件，通过了不屏蔽规则的完整 ICE 套件，零错误、四条预期 ICE91 警告，验证未改变包字节。该包尚未安装、未完成全生命周期测试，也未提升发布。生产安装器定义尚未采用这一条件检查。

原始安装包与诊断记录保留在 Git 忽略的 `artifacts/validation/3.4.1/` 目录下。

## 发布状态

最终候选已明确标识，并通过本地 UAC 迁移及普通用户启动检查。托管生命周期证据适用于上述较早包，其应用源码与 MSI 定义相同。隔离配置下的基本英文和简体中文设置界面检查已通过。最终 CI 原文件当时保留用于提升，不重新构建；此历史记录早于 3.4.2 的公开发布。
